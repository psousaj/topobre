{
    "name": "@topobre/bullmq",
    "version": "0.0.0",
    "main": "src/index.ts",
    "scripts": {
        "build:tsc": "tsc -b",
        "build:tsup": "tsup",
        "build": "pnpm build:tsc && pnpm build:tsup"
    },
    "dependencies": {
        "bullmq": "^4.17.0",
        "@topobre/env": "workspace:*"
    },
    "devDependencies": {
        "@topobre/tsconfig": "workspace:*"
    }
}