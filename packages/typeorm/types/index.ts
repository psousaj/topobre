export enum TransactionStatus {
    PENDING = 'pending',
    PAID = 'paid',
    CANCELED = 'canceled',
}

export enum TransactionType {
    INCOME = 'income',
    EXPENSE = 'expense',
}

export enum UserRole {
    ADMIN = 'ADMIN',
    MEMBER = 'MEMBER',
}
